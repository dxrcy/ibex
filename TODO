CHANGE EVERYTHING!
---------------
use context type
use `<base>` and remove `url!` ?


MACRO
---------------
refactor everything
    return result, not panic
    
accept `else-if` statements


ROCKET SSR
---------------
support rocket ssr!

impl Responder for Document



